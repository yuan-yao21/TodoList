<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FakeNote" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/basic_color</item>
        <item name="colorPrimaryVariant">@color/basic_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FakeNote.ToolBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="overlapAnchor">false</item> <!--是为了将menu放到三个点的下面-->
        <item name="android:textColorPrimary">@android:color/white
        </item> <!--三个点的颜色，如果下面不设置子标题颜色那么子标题字体颜色就会是三个点的颜色-->
        <item name="android:textColorSecondary">@android:color/black</item> <!--子标题字体颜色-->
        <item name="android:textSize">18sp</item> <!--修改字体大小-->
        <item name="android:textColor">@color/black</item> <!--menu字体颜色-->
        <!--<item name="android:itemBackground">@color/purple_200</item>修改item背景颜色-->
    </style>

    <style name="Theme.FakeNote.Fullscreen" parent="Theme.FakeNote">
        <item name="android:actionBarStyle">@style/Widget.Theme.FakeNote.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.FakeNote.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
    <style name="Theme.FakeNote.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.FakeNote.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.FakeNote.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>